# AI usage

The challenge requests the original exported session logs from every AI assistant used. Add those exports to an `ai-sessions/` directory before submission.

Do not replace the original exports with this summary. This file only makes the expected location obvious to reviewers.
